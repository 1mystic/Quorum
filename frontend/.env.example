# Base URL for the real backend, no trailing slash. Everything under
# frontend/src/api/ builds on this (see src/api/client.js). Defaults to
# http://localhost:8000/api if unset - fine for local `uv run uvicorn`.
VITE_API_BASE_URL=http://localhost:8000/api
